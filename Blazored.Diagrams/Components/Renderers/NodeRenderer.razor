@foreach (var node in Nodes)
{
    <NodeContainer Node="node"></NodeContainer>
}

@code {

    /// <summary>
    /// Nodes to be rendered.
    /// </summary>
    [Parameter]
    public required IEnumerable<INode> Nodes { get; set; }
}