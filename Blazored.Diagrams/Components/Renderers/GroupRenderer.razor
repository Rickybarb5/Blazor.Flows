@foreach (var group in Groups)
{
    <GroupContainer Group="group"></GroupContainer>
}

@code {

    /// <summary>
    /// Groups to be rendered.
    /// </summary>
    [Parameter]
    [EditorRequired]
    public required IEnumerable<IGroup> Groups { get; set; }
}